[project]
name = "quick-deploy"
version = "0.1.0"
description = "Workspace-level uv project for quick-deploy tooling."
requires-python = ">=3.10,<3.11"
dependencies = [
  "pip>=26.0.0",
]

[tool.uv]
package = false
